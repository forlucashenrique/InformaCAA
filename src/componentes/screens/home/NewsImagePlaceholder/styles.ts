import { StyleSheet } from "react-native";

export const NewsImagePlaceholderStyle = StyleSheet.create({
    container: {
        width: '100%',
        borderRadius: 12,
        height: 200,
        marginBottom: 40,
    },

    img: {
        width: '100%',
        height: '100%',
        position: 'absolute',
    }
})